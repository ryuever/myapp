<%= provide(:title, "All users") %>
<h1>All users</h1>
<%= will_paginate @users,  :outer_window => 1, :inner_window => 1 %>
<ul class="users">
  <%= render @users %>
</ul>
<%= will_paginate @users, :outer_window => 2%>
